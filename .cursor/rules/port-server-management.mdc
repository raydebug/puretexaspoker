# Port and Server Management

- Always ensure only one backend server is running on port 3001 to avoid EADDRINUSE errors.
- Restart servers by default when port conflicts occur.
- Check current path before starting each server.
description:
globs:
alwaysApply: false
---
